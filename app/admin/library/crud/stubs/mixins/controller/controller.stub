<?php

namespace {%namespace%};
{%use%}
use {%backendBaseClass%};

/**
 * {%tableComment%}
 */
class {%className%} extends {%backendBaseClassName%}
{
    /**
     * {%modelName%}模型对象
     * @var object
     * @phpstan-var \{%modelNamespace%}\{%modelName%}
     */
    protected object $model;
{%attr%}{%initialize%}
{%methods%}

    /**
     * 若需重写查看、编辑、删除等方法，请复制 @see \{%backendTraitClass%} 中对应的方法至此进行重写
     */
}